# 字符串对象
slice(start,end).....[start,end)可以取负值  
substring(start,end)...从最小到最大截取  
substr(start,length) 不建议使用

charCodeAt(index)//返回字符串指定index的ascii值  
String.fromCharCode(65)//返回ascii为65的字符  
charAt(index)//返回index的字符  
indexOf('hello'); //字符第一次出现的位置，没有返回－1； 
lastIndexOf('hello')//字符最后一次出现的位置，没有返回－1；
mearch('hello');  //与indexOf相似  
concat();//连接字符串
split(‘’);//用‘’符号分割字符串  默认为，分割
toUpperCase()_toLowerCase()  
repalce(str1,str2)//str2替换str1;

**concat,split,join,sort不改变数组或者字符串本身。  
sort/reverse  str不改变   arr改变